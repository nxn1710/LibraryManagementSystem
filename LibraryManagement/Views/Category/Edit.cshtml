
@model LibraryManagement.Models.BookCategory
<div class="row">
    <div class="col-lg-6">
        <div class="panel panel-info">
            <div class="panel-heading">
                Update Category ID @Model.id
            </div>
            <div class="panel-body">
                <div class="row">
                    <div class="col-lg-12">
                        @using (Html.BeginForm("Edit", "Category", FormMethod.Post))
                        {
                            @Html.AntiForgeryToken()
                            <div class="form-group">
                                @Html.HiddenFor(a => a.id)
                                @Html.LabelFor(a => a.category_name, "Category Name")
                                @Html.TextBoxFor(a => a.category_name, null, new { @class = "form-control" })
                                @Html.ValidationMessageFor(m => m.category_name, null, new { @class = "text-danger" })
                            </div>
                            <div class="form-group">
                                @Html.HiddenFor(a => a.id)
                                @Html.LabelFor(a => a.description, "Description")
                                @Html.TextBoxFor(a => a.description, null, new { @class = "form-control" })
                                @Html.ValidationMessageFor(m => m.description, null, new { @class = "text-danger" })
                            </div>
                            <button type="submit" class="btn btn-info">Update</button>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

