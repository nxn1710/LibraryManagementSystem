@model PagedList.IPagedList<LibraryManagement.Models.StaffAccount>
@using PagedList.Mvc;

<div class="row">
    @if (TempData["message"] != null) {
        var className = "alert text-center " + (@TempData["error"] != null ? "alert-danger" : "alert-success");
        <p class="@className">
            @TempData["message"]
        </p>
    }
    <div class="col-sm-9 mb-1">
        @using (Html.BeginForm("Index", "Staff", FormMethod.Get)) {
            <div class="col-sm-5 pl-0">
                @Html.TextBox("key", null, new { @class = "form-control input-sm" })
            </div>
            <div class="col-sm-7">
                <input class="btn btn-primary" type="submit" value="Search" />
                @Html.ActionLink("Clear", "Index", "Staff", null, new { @class = "btn btn-default" })
                <a href="@Url.Action("Add", "Staff")" class="btn btn-success">
                    <i class="fa fa-plus"></i>
                    Add New
                </a>
            </div>
        }
    </div>
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                Staff account
            </div>
            <div class="panel-body">
                <div class="table-responsive">
                    <table class="table table-striped table-bordered table-hover"
                           id="dataTables-example">
                        <thead>
                            <tr class="center">
                                <th>Staff ID</th>
                                <th>Staff account name</th>
                                <th>Staff full name</th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var staff in Model) {
                            <tr>

                                <td>@staff.id</td>
                                <td> @staff.username</td>
                                <td> @staff.fullname</td>
                                <td>
                                    <a href="@Url.Action("Edit", "Staff", new {@staff.id})" class="btn btn-primary"><i class="fa fa-pencil-square-o"></i> </a>
                                    <a href="@Url.Action("Delete", "Staff", new {@staff.id})" class="btn btn-danger"><i class="fa fa-trash"></i></a>
                                </td>
                            </tr>
                            }

                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
<div class="row" style="text-align: center;">

    Page @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) / @Model.PageCount
    @Html.PagedListPager(Model, page => Url.Action("Index",
        new { page, key = ViewBag.searchValue }))

</div>